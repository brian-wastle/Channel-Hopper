
<div>
    <a href="/community/{{community.id}}/"><h1>{{community.name}}</h1></a>
    <h3>{{subject}}</h3>
    <br>
    <img src="{{author.avatarPath}}" alt="user avatar" width="30" height="30">
    <span>{{author.name}} on {{format_date date_created}}</span>
    



        <div class="col-md-6 blog-list">
            <h3>Replies:</h3>

            {{#each posts as |post|}}
            <div class="row mb-2">
            {{body}}
            <br>
            </div>
            {{/each}}
        </div>

    <div>
        <form class="form new-comment-form">
            <div class="form-group">
                <label for="post-body">Add a comment:</label>
                <br>
                <textarea class="form-input" id="post-body" name="post-body" rows="10" cols="40"></textarea>
            </div>
            <div class="form-group">
                <button type="submit" class="btn btn-primary">Add Comment</button>
            </div>
        </form>
    </div>



</div>

  <script src="/js/new-post.js"></script>